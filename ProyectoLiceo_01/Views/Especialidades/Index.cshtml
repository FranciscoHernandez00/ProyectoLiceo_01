@model IEnumerable<ProyectoLiceo_01.Models.Especialidades>

@{
    ViewBag.Title = "Index";
    Layout = null;
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NombreEspecialidad)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Descripcion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NombreEspecialidad)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Descripcion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.EspecialidadID }) |
            @Html.ActionLink("Details", "Details", new { id=item.EspecialidadID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.EspecialidadID })
        </td>
    </tr>
}

</table>
